<?xml version="1.0" encoding="UTF-8"?>
<ivy-settings>
  <settings defaultResolver="main" />
  <!--Authentication required for publishing (deployment). 'Artifactory Realm' is the realm used by Artifactory so don't change it.-->
  <credentials host="localhost" realm="Artifactory Realm" username="${security.getCurrentUsername()}" passwd="${security.getEncryptedPassword()}" />
  <resolvers>
    <chain name="main">
	
	<!--
      <ibiblio name="public" m2compatible="true" root="http://localhost:8080/artifactory/libs-release" />
	-->  
	 <resolvers>
		<url name="artifactory-publish">
			<!-- You can use  m2compatible="true" instead of specifying your own pattern -->
			<artifact pattern="http://localhost:8080/artifactory/libs-snapshot-local/[organization]/[module]/[revision]/[artifact]-[revision].[ext]"/>
			<ivy pattern="http://localhost:8080/artifactory/libs-snapshot-local/[organization]/[module]/[revision]/ivy-[revision].xml" />
		</url>
	</resolvers> 
	  
    </chain>
  </resolvers>
</ivy-settings>
